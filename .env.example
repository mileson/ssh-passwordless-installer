# Optional Developer ID identity used to sign macOS app bundles.
CODESIGN_IDENTITY=Developer ID Application: Your Name (TEAMID)

# Optional notarytool keychain profile name used for notarization.
NOTARY_PROFILE=YOUR_NOTARY_PROFILE
